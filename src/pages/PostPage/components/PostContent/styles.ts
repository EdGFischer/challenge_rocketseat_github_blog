import styled from 'styled-components'

export const PostContatentContainer = styled.main`
  max-width: 864px;
  width: 100%;
  padding: 2.5rem;
  white-space: pre-wrap;
  overflow: hidden;
  color: ${(props) => props.theme['base-text']};
`
